package com.example.terrain.repositories;

import com.example.terrain.bean.Pack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PackR extends JpaRepository<Pack,Integer> {
}
